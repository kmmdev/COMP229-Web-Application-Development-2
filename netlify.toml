# Netlify deployment configuration
[build]
  base = "client/"
  publish = "dist"
  command = "npm install && npm run build"

[context.production]
  command = "npm install && npm run build"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200